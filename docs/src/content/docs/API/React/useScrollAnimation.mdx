---
title: useScrollAnimation
description: A custom react hook to make it easy to create scroll animations in React.
sidebar:
  order: 14
---

A custom react hook to make it easy to create scroll animations in React.

Based on [scrollAnimation](/api/plugins/scrollanimation)

```ts wrap title="syntax"
function useScrollAnimation(options: ScrollAnimationOptions, deps: React.DependencyList = []): void;
```

See: [ScrollElementEdge](/animare/api/enums/scrollelementedge/), [ScrollAxis](/animare/api/enums/scrollaxis/)

## Usage

```ts title="example" frame="terminal"
import { useScrollAnimation } from 'animare/react';

function MyComponent() {
  const timeline = useAnimare(() => {
    // ...
  }, []);

  useScrollAnimation({
    timeline: timeline,
    element: document.querySelector<HTMLDivElement>('.block')!,
    start: ScrollElementEdge.Bottom,
    end: ScrollElementEdge.Top,
    startOffset: 100,
  });
}
```
