---
title: Loop
description: Create a game loop.
sidebar:
  order: 6
---

import { Badge } from '@astrojs/starlight/components';

Create a game loop.

```ts title="syntax"
function loop(onUpdateCallback: (delta: number) => void): () => void;
```

## Usage

```ts title="example" frame="terminal"
import animare from 'animare';

const stop = animare.loop(delta => {
  // do something
});

stop(); // stop the loop if needed
```

To use with React see: [useLoop](/animare/api/react/useloop)
